<%
    //script : 매크로 js, python 등등| interface : 중간기능
    // javascript : script < client
    // node.js (vue,react) < front + end
    // restful backend json > front json > view(vue,spring등) + ajax(비동기)






%>